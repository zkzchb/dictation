# ── Claude Code 工具文件（本地专属）────────────────────────────────
.claude/
CLAUDE.md

# ── 废弃脚本（含明文 API 密钥，不进版本库）──────────────────────────
attic/

# ── Python 运行时 ─────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
venv/
_testenv/
.testenv/
_build_venv/
*.egg-info/
.venv/

# ── 密钥与环境变量 ─────────────────────────────────────────────────
.env
*.env.local

# sync-slices.sh --pull 覆盖前的切片备份
shared/web/audio.bak_*/

# 部署配置：含明文密钥，只提交 *.example 模板
deploy/vps.env
deploy/cloudflare.env
deploy/local.env

# 本地试跑用的环境变量（含密钥，由 local-install.sh 生成）
.local-run.env
.local-svc.env
.runtime/

# ── 数据库（含用户学习数据，不应进版本库）────────────────────────────
*.db
*.db.backup

# ── 音频文件 ───────────────────────────────────────────────────────
# 正式音频位于外部内容仓库；程序仓库中的运行副本和构建产物不提交。
shared/web/audio/
v2/audio/
v3/public/audio/
*.mp3

# ── V3 stage 产物（由 tools/stage.py 生成，不提交）─────────────────
# v3/public/index.html 来自 shared/web/，wrangler 部署前需先运行 stage
v3/public/

# ── 日志与调试临时文件 ─────────────────────────────────────────────
api.log
*.log
manifest.json
_test*.db
_verify*.py
_check*.js
_inspect*.py

# ── 旧版备份 ───────────────────────────────────────────────────────
*.bak

# ── 操作系统杂项 ───────────────────────────────────────────────────
.DS_Store
Thumbs.db

# Excel 临时文件
~$*.xlsx
~$*.xls

# 原始词表 Excel 不入库
*.xlsx
*.xlsx.bak*

# 设计草稿不入库；正式冻结/运维文档例外。
docs/*
!docs/V2-FREEZE.md
!docs/V2-REPRODUCIBLE-INSTALL.md
!docs/V2.1-OSS-ROADMAP.md
!docs/V2.1-OSS-AUDIT.md
!docs/CONTENT-PACK-SPEC.md
!docs/CONTENT-LICENSING.md
!docs/REPOSITORY-ARCHITECTURE.md
!docs/RELEASE-CHECKLIST.md
!docs/verification/
!docs/verification/**

# 一次性/过期工具（本地保留，不入库）
tools/add_lesson_cols.py
shared/tools/fill_word2write.py
shared/tools/wordlist_template.csv

# 过期的分版本建库脚本（已由 shared/init_db.py 统一取代）
v2/init_db.py

# stage.py 生成物（母本在 shared/web/，部署前运行 stage 重新生成）
v2/dictation_www/
v2/audio/

# export_d1.py 生成物（部署前重新生成）
v3/migrations/0002_seed.sql
v3/.content-runtime.sql

# 本地部署脚本（不入库）
deploy-local.sh
