# V2 依赖 —— 预录切片版，运行时不调 TTS
# 仅录音工作台 /api/studio/split 用到 pydub（需系统装 ffmpeg）
#
# V2 冻结版直接依赖。Ubuntu 24.04 / Python 3.12 安装器只使用 v2/wheelhouse，
# 不访问 pip 软件源。Docker 版本以这份清单为基线，不安装“当时最新版”。
fastapi==0.141.1
uvicorn==0.52.3
pydantic==2.13.4
pydub==0.25.1

# /api/studio/split 收的是 multipart/form-data（录音 blob）。starlette 解析
# 表单要靠它，缺了就在 await request.form() 处抛 AssertionError，前端只看到
# 500，而 catch 里那句「服务器需装 ffmpeg/pydub」会把人往错方向引。
# fastapi 不会自动带上它 —— 要装 fastapi[standard] 才有，这里显式钉住。
python-multipart==0.0.32

# Python 3.13 起标准库移除了 audioop，而 pydub 依赖它。
# audioop-lts 是官方替代实现，仅在 3.13+ 上安装。
audioop-lts==0.2.1; python_version >= "3.13"

# 冻结时在 Python 3.12 解析出的运行时传递依赖。显式列出，避免未来重新安装
# 时 FastAPI/uvicorn 的宽范围依赖漂移；升级必须走完整回归测试。
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
click==8.4.2
h11==0.16.0
idna==3.19
pydantic-core==2.46.4
starlette==1.6.0
typing-extensions==4.16.0
typing-inspection==0.4.4
