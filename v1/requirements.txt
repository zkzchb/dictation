# V1 依赖 —— 运行时调用有道 TTS + pydub 拼接音频（需系统装 ffmpeg）
#
# 版本说明：不再钉死旧版。旧的 pydantic==2.5.2 在 Python 3.13+ 上会尝试
# 从源码编译 pydantic-core（需要 Rust 工具链），在新系统上必然失败。
# 这里放宽下限，让 pip 直接装带预编译 wheel 的新版本。
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.9
requests>=2.32
pydub>=0.25.1

# Python 3.13 起标准库移除了 audioop，而 pydub 依赖它。
# audioop-lts 是官方的替代实现，仅在 3.13+ 上安装。
audioop-lts>=0.2.1; python_version >= "3.13"
